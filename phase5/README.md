# Phase 5: Frontend

Streamlit UI for the Groww MF FAQ Chatbot.

**Run from project root:**

```bash
streamlit run phase5/streamlit_app.py
```

Uses `CHAT_API_URL` (default `http://localhost:8000`) for Phase 4 API. If the API is unavailable, falls back to Phase 3 RAG in-process.
